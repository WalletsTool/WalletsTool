# 窗口命名规范

本文档描述了WalletsTool应用程序中窗口命名的规范和最佳实践。

## 概述

从版本2.0开始，WalletsTool采用了统一的窗口命名系统，使窗口标题更加优雅、信息丰富且易于管理。

## 命名结构

### 基础格式

```
WalletsTool - {图标} {功能名称} | {业务标识} [{实例编号}]
```

### 组成部分

1. **应用名称**: `WalletsTool`
2. **功能图标**: 根据功能类型显示的emoji图标
3. **功能名称**: 功能模块的中文名称
4. **业务标识** (可选): 用于区分多开窗口的具体用途
5. **实例编号** (可选): 多窗口时的序号，从1开始

## 功能模块配置

| 模块Key | 功能名称 | 图标 | 默认业务标识 |
|---------|---------|------|-------------|
| `transfer` | 批量转账 | 💸 | A组, B组, C组, 冷钱包, 热钱包, 交易所, 个人, 测试 |
| `balance` | 余额查询 | 💰 | 大额地址, 空投检查, 代币分布, 资产统计, VIP检查 |
| `monitor` | 链上监控 | 👁️ | 大额监控, 新地址, 活动监控, 异常监控 |

## 使用示例

### 1. 基础窗口 (单窗口)

```javascript
// 创建单一功能窗口
const title = WINDOW_CONFIG.generateTitle('transfer');
// 结果: "WalletsTool - 💸 批量转账"
```

### 2. 带业务标识的窗口

```javascript
// 为窗口添加业务标识
const title = WINDOW_CONFIG.generateTitle('transfer', '热钱包');
// 结果: "WalletsTool - 💸 批量转账 | 热钱包"
```

### 3. 多窗口实例

```javascript
// 多开窗口，自动编号
const title1 = WINDOW_CONFIG.generateTitle('transfer', '热钱包', 1);
const title2 = WINDOW_CONFIG.generateTitle('transfer', '热钱包', 2);
// 结果1: "WalletsTool - 💸 批量转账 | 热钱包 [1]"
// 结果2: "WalletsTool - 💸 批量转账 | 热钱包 [2]"
```

### 4. 托盘菜单窗口

```javascript
// 托盘创建的窗口
const trayTitle = WINDOW_CONFIG.generateTrayTitle('transfer', 3);
// 结果: "🔗 💸 转账 [3]"
```

### 5. 自动生成业务标识

```javascript
// 根据已有标识自动建议新的业务标识
const existingLabels = ['A组', 'B组'];
const newLabel = WINDOW_CONFIG.suggestBusinessLabel('transfer', existingLabels);
// 结果: "C组"
```

## API 参考

### `WINDOW_CONFIG.generateTitle(moduleKey, businessLabel?, instanceNum?)`

生成功能窗口的完整标题。

**参数:**
- `moduleKey` (string): 功能模块标识 ('transfer', 'balance', 'monitor')
- `businessLabel` (string, optional): 业务标识
- `instanceNum` (number, optional): 实例编号，默认1

**返回:** string - 格式化的窗口标题

### `WINDOW_CONFIG.generateTrayTitle(moduleKey, instanceNum)`

生成托盘菜单创建窗口的标题。

**参数:**
- `moduleKey` (string): 功能模块标识
- `instanceNum` (number): 实例编号

**返回:** string - 格式化的托盘窗口标题

### `WINDOW_CONFIG.generateLabel(pageName, instanceNum)`

生成窗口的内部标签 (label)。

**参数:**
- `pageName` (string): 页面名称
- `instanceNum` (number): 实例编号

**返回:** string - 窗口标签

### `WINDOW_CONFIG.suggestBusinessLabel(moduleKey, existingLabels)`

根据已有标签自动建议新的业务标识。

**参数:**
- `moduleKey` (string): 功能模块标识
- `existingLabels` (string[]): 已使用的标签列表

**返回:** string - 建议的新标签

## 分隔符说明

- **主分隔符**: ` | ` - 分隔功能名称和业务标识
- **实例分隔符**: ` [` 和 `]` - 包裹实例编号

示例:
```
WalletsTool - 💸 批量转账 | 热钱包 [2]
           ↑     ↑      ↑  ↑      ↑  ↑
           |     |      |  |      |  实例编号
           |     |      |  业务标识
           |     功能名称
           应用名称
```

## 最佳实践

### 1. 业务标识命名

为多开窗口选择清晰、有意义的业务标识:
- 使用钱包组名称: `热钱包`, `冷钱包`, `交易所`
- 使用用途描述: `空投检查`, `资产统计`
- 使用链名称: `Ethereum`, `Solana`, `BSC`

### 2. 窗口创建时机

```javascript
// 推荐: 创建窗口时立即设置标题
const webview = new WebviewWindow(windowLabel, {
  title: WINDOW_CONFIG.generateTitle('transfer', '热钱包', 1),
  // ... 其他配置
});
```

### 3. 动态更新窗口标题

根据窗口状态动态更新标题:

```javascript
// 任务执行中
const windowTitle = WINDOW_CONFIG.generateTitle('transfer', '热钱包', 1);
const statusTitle = `${windowTitle} (执行中)`;

// 更新窗口标题
await currentWindow.setTitle(statusTitle);
```

## 注意事项

1. **唯一性**: 确保同一功能的多个窗口有不同的业务标识
2. **可读性**: 业务标识应简洁明了，避免过长
3. **一致性**: 同一项目中使用统一的命名风格
4. **国际化**: 如需支持多语言，可扩展配置模块

## 文件位置

窗口命名配置模块位于: `src/utils/windowNames.js`
